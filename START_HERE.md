# ⚡ БЫСТРЫЙ СТАРТ

## Что нужно сделать ПРЯМО СЕЙЧАС:

### 1️⃣ Скопировать файлы

Запусти файл `copy-assets.bat` двойным кликом.

Он автоматически скопирует:
- ✅ CSS файлы → src/styles/
- ✅ Шрифты → public/fonts/
- ✅ Изображения → public/images/

### 2️⃣ Установить зависимости

Открой командную строку в папке проекта и выполни:

```bash
cd C:\Users\x\Desktop\otvetai\nextjs-askme
npm install
```

### 3️⃣ Настроить импорты CSS

Открой файл `src/app/globals.css` и раскомментируй строки:

```css
@import url('../styles/style.css');
@import url('../styles/main.css');
@import url('../styles/modal.css');
/* ... и остальные */
```

### 4️⃣ Запустить проект

```bash
npm run dev
```

Открой http://localhost:3000

---

## ✅ Всё работает?

**Напиши мне когда:**
- ✅ Файлы скопированы
- ✅ npm install завершился
- ✅ Проект запустился на localhost:3000

**И я сразу начну создавать компоненты!** 🚀

---

## 📁 Полезные файлы:

- `NEXT_STEPS.md` - Детальные инструкции
- `COMPONENTS_PLAN.md` - План всех компонентов
- `README.md` - Общая информация о проекте
- `copy-assets.bat` - Скрипт копирования файлов

---

## ❌ Если что-то не работает:

1. **copy-assets.bat выдает ошибку:**
   - Проверь что путь к WordPress проекту правильный
   - Скопируй файлы вручную (инструкция в NEXT_STEPS.md)

2. **npm install не работает:**
   - Установи Node.js: https://nodejs.org/
   - Перезапусти командную строку

3. **Проект не запускается:**
   - Удали папки `.next` и `node_modules`
   - Запусти `npm install` снова

---

**Жду твоего сообщения!** 😊
