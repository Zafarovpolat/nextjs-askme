# 📋 СЛЕДУЮЩИЕ ШАГИ - AskMe Next.js

## ✅ Что уже сделано:

1. ✅ Создана базовая структура Next.js проекта
2. ✅ Настроен TypeScript
3. ✅ Создан layout и главная страница
4. ✅ Подготовлен package.json
5. ✅ Создан скрипт для копирования файлов

---

## 🚀 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС:

### Шаг 1: Скопировать файлы из WordPress

**Вариант A (автоматически):**
Запустите файл `copy-assets.bat` двойным кликом

**Вариант B (вручную):**
1. Скопируйте CSS файлы:
   - Из: `..\_live.mis507nz...\wp-content\themes\askme\assets\css\`
   - В: `src\styles\`

2. Скопируйте шрифты:
   - Из: `...\wp-content\themes\askme\assets\fonts\`
   - В: `public\fonts\`

3. Скопируйте изображения:
   - Из: `...\wp-content\themes\askme\assets\img\`
   - В: `public\images\`

### Шаг 2: Настроить импорты CSS

Откройте `src/app/globals.css` и раскомментируйте строки с импортами:

```css
@import url('../styles/style.css');
@import url('../styles/main.css');
@import url('../styles/modal.css');
@import url('../styles/categories.css');
@import url('../styles/questions.css');
@import url('../styles/profile.css');
@import url('../styles/leaders.css');
@import url('../styles/ask.css');
@import url('../styles/login-auth.css');
@import url('../styles/404.css');
```

### Шаг 3: Установить зависимости

```bash
cd C:\Users\x\Desktop\otvetai\nextjs-askme
npm install
```

### Шаг 4: Запустить проект

```bash
npm run dev
```

Откройте http://localhost:3000

---

## 📝 ДАЛЬНЕЙШИЕ ЗАДАЧИ:

После того как проект запустится, я помогу тебе:

1. **Создать Layout компоненты:**
   - Header (шапка с поиском)
   - Footer (подвал)
   - Sidebar Left (навигация)
   - Sidebar Right (топ категории, топ пользователи)

2. **Создать UI компоненты:**
   - QuestionCard (карточка вопроса)
   - UserCard (карточка пользователя)
   - CategoryCard (карточка категории)
   - CommentItem (ответ)
   - Modal (модальные окна)
   - Button (кнопки)

3. **Создать страницы:**
   - Главная (/)
   - Категории (/categories)
   - Вопрос (/question/[slug])
   - Задать вопрос (/ask)
   - Профиль (/profile/[username])
   - Лидеры (/leaders)
   - Вход (/login)
   - Регистрация (/signup)

4. **Добавить моковые данные:**
   - Примеры вопросов
   - Примеры пользователей
   - Примеры категорий

---

## ❓ Если что-то не работает:

1. **Ошибки при npm install:**
   - Проверьте что Node.js установлен: `node --version`
   - Проверьте что npm установлен: `npm --version`

2. **Стили не загружаются:**
   - Проверьте что CSS файлы скопированы в `src/styles/`
   - Проверьте что импорты раскомментированы в `globals.css`

3. **Проект не запускается:**
   - Удалите `node_modules` и `.next`
   - Запустите `npm install` заново

---

## 📞 Готов продолжить?

Как только файлы скопированы и проект запущен - скажи мне, и я начну создавать компоненты! 🚀
